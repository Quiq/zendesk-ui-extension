{
  "name" : "zendeskUIExtension",
  "deploymentPipeline" : {
    "versionCommand" : "cat package.json | grep version | sed 's/[^0-9.]*\\([0-9.]*\\).*/\\1/'",
    "buildCommand" : "yarn && yarn build",
    "unitTestCommand" : "echo 'passed'"
  }
}
